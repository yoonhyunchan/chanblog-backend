apiVersion: apps/v1
kind: Deployment
metadata:
  name: login
  namespace: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: login
  template:
    metadata:
      labels:
        app: login
    spec:
      containers:
      - name: login
        image: harbor.chanandy.internal/chanproject/login:latest
        ports:
        - containerPort: 5000
        volumeMounts:
        - name: config-volume
          mountPath: /app/.env
          subPath: .env
      volumes:
      - name: config-volume
        configMap:
          name: dbconfig
          items:
          - key: .env
            path: .env
